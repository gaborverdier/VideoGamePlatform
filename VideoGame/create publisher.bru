meta {
  name: create publisher
  type: http
  seq: 3
}

post {
  url: http://localhost:8083/api/publishers
  body: json
  auth: inherit
}

body:json {
  {
    "name" : "Nintendo"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
